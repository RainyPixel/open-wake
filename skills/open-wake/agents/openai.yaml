interface:
  display_name: "Open Wake"
  short_description: "Run long work without Codex polling"
  default_prompt: "Use $open-wake to run or observe this long-running task without model-side polling."
